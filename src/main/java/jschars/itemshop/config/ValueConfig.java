package jschars.itemshop.config;

import jschars.itemshop.Itemshop;

public class ValueConfig extends CustomConfig {
    public ValueConfig(Itemshop plugin, String name) {
        super(plugin, name);
    }
}
